thing:
  url: https://metalab.at/things/keys/door
  token: "changeme"
  refresh_secs: 60

persistence:
  path: key_list.bin

logging:
  appenders:
    stdout:
      kind: console
      # encoder:
      #   kind: json

  root:
    level: debug
    appenders:
      - stdout
